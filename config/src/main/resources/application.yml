spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/GiorgiSheklashvili/smart-order-config.git"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true
    bus:
      enabled: true
      id: ${spring.application.name}:${spring.profiles.active}:${random.uuid}
  kafka:
    bootstrap-servers: 'localhost:9092'
management:
  endpoints:
    web:
      exposure:
        include: 'bus-refresh,monitor'
server:
  port: 8071